.env

__pycache__/
*.pyc

# PDFs (large binaries; keep local copies only)
*.pdf

# Video (local downloads / compilations; keep out of git)
*.mp4
*.mkv
*.webm

# yt-dlp download archive (per compilation folder; local skip list)
**/.yt-dlp-archive.txt

# Parsed book text extracts (large; regenerate locally)
books/parsed/
